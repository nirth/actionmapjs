// @flow

import type {StoreInstructions} from './types'

type EventMapAndInitialState = {eventMap: EventMap, initialState: State}

const toEventMapAndInitialState = (storeInstructions: StoreInstructions): EventMapAndInitialState => {
  return null
}

export default toEventMapAndInitialState
